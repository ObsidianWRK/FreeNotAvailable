import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Ekthesis — FREE',
  description: 'A proposition for the Other World. The investment thesis for FREE, a Detroit R&B artist building a visual universe.',
}

export default function EkthesisLayout({ children }: { children: React.ReactNode }) {
  return children
}
